apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pipeline-reader
rules:
- apiGroups: ["tekton.dev"]
  resources: ["pipelines", "persistentvolumeclaims"]
  verbs: ["list", "get"]